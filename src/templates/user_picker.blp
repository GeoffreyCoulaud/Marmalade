using Gtk 4.0;
using Adw 1;

template $MarmaladeUserPicker : Gtk.Box {
  orientation: vertical;
  valign: start;
  Adw.Carousel carousel {
    halign: center;
  }
  Adw.CarouselIndicatorDots {
    carousel: carousel;
    halign: center;
  }
}