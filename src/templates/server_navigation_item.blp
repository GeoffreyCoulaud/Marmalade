using Gtk 4.0;
using Adw 1;

template $MarmaladeServerNavigationItem : ListBoxRow {
  child: Box {
  spacing: 10;
    Image image {}
    Label label {
      wrap: true;
      wrap-mode: word;
    }
  };
}