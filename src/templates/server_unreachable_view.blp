using Gtk 4.0;
using Adw 1;

template $MarmaladeServerUnreachableView : Adw.Bin {
  child: Adw.StatusPage status_page {
    icon-name: "network-wired-no-route-symbolic";
    title: _("Unreachable Server");
  };
}