using Gtk 4.0;

template $MarmaladeUserPickerPage : Gtk.FlowBox {
  // Items spacing
  column-spacing: 8;
  row-spacing: 8;
  // Page margin
  margin-top: 8;
  margin-bottom: 8;
  margin-start: 8;
  margin-end: 8;
}